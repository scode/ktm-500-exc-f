#!/usr/bin/env bash
set -euo pipefail

MDBOOK_VERSION="0.4.43"

mkdir -p ~/bin
curl -sSL "https://github.com/rust-lang/mdBook/releases/download/v${MDBOOK_VERSION}/mdbook-v${MDBOOK_VERSION}-x86_64-unknown-linux-gnu.tar.gz" | tar -xz -C ~/bin
export PATH="$HOME/bin:$PATH"

mdbook build
